project Bridge is
   for Source_Dirs use ("src/messages", "src/converter", "src/codec", "src");
   for Main use ("bridge.adb");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   
   package Naming is
      for Body_Suffix ("Ada") use ".adb";
      for Spec_Suffix ("Ada") use ".ads";
      -- for Dot_Replacement use "/";
   end Naming;

end Bridge;
